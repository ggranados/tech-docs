# Quickstart: Verifying Algorithms Content

1. Confirm 8 files exist under `docs/pages/algorithms/`.
2. Confirm each has complexity notes and a worked example.
3. Confirm `get-started.md`'s Algorithms section has no stray `)` typos and no unlinked bullets among the 10 listed.
4. Re-run the link-resolution check across `docs/pages/algorithms/**/*.md`.
